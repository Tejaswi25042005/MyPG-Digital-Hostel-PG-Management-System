<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <RelativeLayout
        android:id="@+id/RequstLayout"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        tools:context=".auth.RequestRegisterActivity">

        <!-- Title -->
        <TextView
            android:id="@+id/tvTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Request Owner Registration"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_centerHorizontal="true" />

        <!-- Full Name -->
        <EditText
            android:id="@+id/etFullName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvTitle"
            android:layout_marginTop="24dp"
            android:hint="Full Name"
            android:background="@drawable/et_background" />

        <!-- Email -->
        <EditText
            android:id="@+id/etEmail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/etFullName"
            android:layout_marginTop="12dp"
            android:hint="Email Address"
            android:inputType="textEmailAddress"
            android:background="@drawable/et_background" />

        <TextView
            android:id="@+id/tvMobile"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/etEmail"
            android:layout_marginTop="12dp"
            android:padding="12dp"
            android:text="Mobile Number"
            android:textColor="#333333"
            android:textSize="14sp"
            android:background="@drawable/et_background" />


        <!-- PG Name -->
        <EditText
            android:id="@+id/etPgName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvMobile"
            android:layout_marginTop="12dp"
            android:hint="PG Name"
            android:background="@drawable/et_background" />

        <!-- PG Type -->
        <EditText
            android:id="@+id/etPgType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/etPgName"
            android:layout_marginTop="12dp"
            android:hint="PG Type (Men / Women / Co-Living)"
            android:background="@drawable/et_background" />

        <!-- Add Location Row -->
        <LinearLayout
            android:id="@+id/layoutLocation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/etPgType"
            android:layout_marginTop="16dp"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/imgAddLocation"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:src="@drawable/ic_location"
                android:contentDescription="Add Location" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="12dp"
                android:text="Add PG Location"
                android:textStyle="bold" />
        </LinearLayout>

        <!-- Address Display -->
        <TextView
            android:id="@+id/tvAddress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/layoutLocation"
            android:layout_marginTop="12dp"
            android:padding="12dp"
            android:text="Full address will appear here"
            android:textColor="#333333"
            android:textSize="14sp"
            android:background="@drawable/et_background"
            android:minLines="2"
            android:maxLines="5"
            android:ellipsize="end"
            android:textIsSelectable="true" />

        <!-- Submit Button -->
        <Button
            android:id="@+id/btnSubmit"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvAddress"
            android:layout_marginTop="24dp"
            android:text="Submit Request"
            android:textAllCaps="false"
            android:backgroundTint="@color/purple_500"
            android:textColor="@android:color/white" />

    </RelativeLayout>
</ScrollView>
